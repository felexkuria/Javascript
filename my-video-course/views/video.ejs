<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= video.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">
  <div class="container mx-auto p-4">
    <h1 class="text-3xl font-bold mb-4 text-center"><%= video.title %></h1>
    <div class="max-w-4xl mx-auto bg-white rounded-lg shadow-lg overflow-hidden">
      <div class="relative">
        <video class="w-full h-auto" controls>
          <source src="/videos/<%= video.videoUrl %>" type="video/mp4">
          <track kind="captions" label="English Captions" default src="/videos/<%= video.captionsUrl %>.vtt" srclang="en">
          Your browser does not support the video tag.
        </video>
        
      </div>
      <div class="p-4">
        <p class="text-gray-600 mb-4"><%= video.description %></p>
        <form action="/videos/<%= video._id %>/watch" method="POST" class="text-center">
          <img src="https://vendorcenter.jumia.com/assets/images/local_red.svg" alt="" srcset="">
          <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition duration-300"> <img src="https://vendorcenter.jumia.com/assets/images/local_green.svg" alt="" srcset="">Mark as Watched</button>
        </form>
        <!-- <div class="mt-4 text-center">
          <a href="/videos/dashboard" class="text-blue-500 hover:underline">Back to Dashboard</a>
        </div> -->
      </div>
    </div>
  </div>
</body>
</html>

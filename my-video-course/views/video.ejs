<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= video.title %></title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex justify-center items-center min-h-screen">
  <div class="bg-white rounded-lg shadow-lg p-6 max-w-lg w-full">
    <h1 class="text-2xl font-bold mb-4"><%= video.title %></h1>
    <video class="w-full mb-4 rounded" controls>
      <source src="/videos/<%= video.videoUrl %>" type="video/mp4">
      Your browser does not support the video tag.
    </video>
    <form action="/videos/<%= video._id %>/watch" method="POST">
        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition duration-300">Mark as Watched</button>
      </form>
      
    <% if (video.watchedAt) { %>
      <p class="mt-4 text-gray-600">Watched on: <%= video.watchedAt.toLocaleString() %></p>
    <% } %>
  </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= video ? video.title : 'Video Not Found' %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans text-gray-900">
  <div class="container mx-auto px-4 py-8">
    <% if (video) { %>
      <% if (video.videoUrl.endsWith('.mp4')) { %>
        <video controls class="w-full h-auto">
         <source src="/videos/<%= courseName %>/<%= video._id %>" type="video/mp4">
          Your browser does not support the video tag.
        </video>
      <% } else { %>
        <div class="text-center p-4 bg-red-100 text-red-700 rounded-md">
          Unsupported file type.
        </div>
      <% } %>
    <% } else { %>
      <h1 class="text-3xl font-bold mb-4 text-red-500">Video Not Found</h1>
      <p class="text-gray-700">The video you are looking for does not exist or has been removed.</p>
    <% } %>
  </div>
</body>
</html>

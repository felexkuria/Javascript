<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= courseName %> Course</title>
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
  <h1 class="text-3xl font-bold mb-4"><%= courseName %> Course</h1>

  <% if (videos.length === 0) { %>
    <div class="text-center p-4 bg-red-100 text-red-700 rounded-md">
      No videos found for this course.
    </div>
  <% } else { %>
    <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <% for (const video of videos) { %>
        <li class="rounded-md overflow-hidden shadow-md">
          <img src="/download.jpg" alt="<%= video.title %>" class="w-full h-48 object-cover">
          <div class="p-4">
            <h3 class="text-lg font-medium mb-2"><%= video.title %></h3>
            <% if (video.videoUrl.endsWith('.pdf')) { %>
              <a href="/pdf/<%= courseName %>/<%= encodeURIComponent(video.basename) %>" target="_blank" class="inline-block px-4 py-2 bg-green-500 text-white font-medium rounded-md hover:bg-green-700">
                Open PDF
              </a>
            <% } else { %>
              <a href="/videos/<%= courseName %>/<%= video._id %>" class="inline-block px-4 py-2 bg-blue-500 text-white font-medium rounded-md hover:bg-blue-700">
                Watch Now
              </a>
            <% } %>
          </div>
        </li>
      <% } %>
    </ul>
  <% } %>

</body>
</html>

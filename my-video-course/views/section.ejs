<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Section: <%= sectionName %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body class="bg-gray-100 flex">
  <div class="w-1/4 bg-white shadow-lg">
    <div class="p-4 border-b">
      <h2 class="text-xl font-semibold text-gray-800">Sections</h2>
    </div>
  
    <ul class="flex flex-col space-y-2">
      <% for (let section in sections) { %>
        <li class="flex items-center py-2 px-4 text-gray-700 hover:bg-gray-200">
          <i class="fas fa-list mr-2 text-lg text-gray-500"></i>
          <button class="flex items-center justify-between w-full py-2 px-4 text-left text-gray-700 hover:bg-gray-200" type="button" data-toggle="collapse" data-target="#collapse-<%= section %>">
            <span><%= section %></span>
            <i class="fas fa-chevron-down text-lg text-gray-500"></i>
          </button>
          <div id="collapse-<%= section %>" class="collapse show">
            </div>
        </li>
      <% } %>
    </ul>
  </div>
  
  
  <div class="w-3/4 p-6">
    <h1 class="text-4xl font-bold mb-8 text-center text-gray-800">Section: <%= sectionName %></h1>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">  <% videos.forEach(video => { %>
        <div class="p-6 bg-white rounded-lg shadow-md hover:shadow-lg hover:bg-gray-100 transition duration-300"> 
           <a href="/videos/<%= video._id %>">
            <img src="/download.jpg" alt="<%= video.title %>" class="w-full h-48 object-cover rounded-lg mb-4">
          </a>
          <h2 class="text-2xl font-semibold text-gray-800 mb-2 truncate"><%= video.title %></h2>
          <p class="text-gray-600 text-sm truncate"><%= video.description %></p> </div>
      <% }) %>
    </div>
  </div>
  
</body>

</html>

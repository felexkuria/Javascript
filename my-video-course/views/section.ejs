<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= courseName %> Sections</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
  <style>
    .progress-circle {
      width: 40px;
      height: 40px;
      position: relative;
    }
    .progress-circle .circle-bg {
      fill: none;
      stroke: #e6e6e6;
      stroke-width: 3.8;
    }
    .progress-circle .circle {
      fill: none;
      stroke-width: 3.8;
      stroke-linecap: round;
      animation: progress 1s ease-out forwards;
    }
    @keyframes progress {
      from {
        stroke-dasharray: 0 100;
      }
    }
  </style>
</head>
<body class="bg-gray-100 font-sans text-gray-900">
  <div class="w-1/4 bg-white shadow-lg p-2">
    <div class="p-4 border-b bg-blue-500 flex justify-between items-center">
      <h2 class="text-xl font-lexend-deca font-semibold text-white">Sections</h2>
      <div class="progress-circle">
        <svg viewBox="0 0 36 36">
          <path class="circle-bg" d="M18 2.0845
                 a 15.9155 15.9155 0 0 1 0 31.831
                 a 15.9155 15.9155 0 0 1 0 -31.831" />
          <path class="circle" stroke-dasharray="<%= (watchedVideos / totalVideos) * 100 %>, 100" d="M18 2.0845
                 a 15.9155 15.9155 0 0 1 0 31.831
                 a 15.9155 15.9155 0 0 1 0 -31.831"
            stroke="<%= (watchedVideos / totalVideos) * 100 %> >= 75 ? 'green' : 'orange' %>" />
        </svg>
      </div>
    </div>
    <div>
      <% for (let section in sections) { %>
        <a href="/section/<%= section %>"
          class="block text-xs truncate py-2 px-4 text-gray-700 font-lexend-deca hover:bg-blue-200 flex items-center">
          <!-- <img src="https://vendorcenter.jumia.com/assets/images/local_red.svg" alt="Section" class="mr-2 w-4 h-4" /> -->
          <span>
            <%= section %>
          </span>
        </a>
        <% } %>
    </div>
  </div>
  <div class="w-3/4 p-6 mx-auto">
    <div class="grid-container">
      <h1 class="text-2xl font-bold mb-8 text-center text-gray-800 flex justify-center items-center">
        Section: <%= courseName %>
        <div class="progress-circle ml-4">
          <svg viewBox="0 0 36 36">
            <path class="circle-bg" d="M18 2.0845a15.9155 15.9155 0 0 1 0 31.831a15.9155 15.9155 0 0 1 0 -31.831" />
            <path class="circle" stroke-dasharray="<%= (watchedVideos / totalVideos) * 100 %>, 100"
                  stroke="<%= (watchedVideos / totalVideos) * 100 %> >= 75 ? 'green' : 'orange' %>"
                  d="M18 2.0845a15.9155 15.9155 0 0 1 0 31.831a15.9155 15.9155 0 0 1 0 -31.831" />
          </svg>
        </div>
        <span class="ml-2 text-lg"><%= Math.round((watchedVideos / totalVideos) * 100) %>%</span>
      </h1>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
      <% videos.forEach(video => { %>
        <% if (video.videoUrl) { %>
          <div class="p-6 bg-white rounded-lg shadow-md hover:shadow-lg hover:bg-blue-100 transition duration-300">
            <a href="/videos/<%= video._id %>">
              <img src="/download.jpg" alt="<%= video.title %>" class="w-full h-auto object-cover rounded-lg mb-4" />
            </a>
            <h2 class="text-xs font-lexend-deca font-semibold text-gray-800 mb-2 truncate">
              <%= video.title %>
            </h2>
            <p class="text-gray-600 font-lexend-deca text-sm truncate">
              <%= video.description %>
            </p>
            <i class="fa-solid <%= video.watched ? 'fa-check text-green-500' : 'fa-xmark text-red-500' %>"></i>
          </div>
        <% } %>
      <% }) %>
    </div>
  </div>
</body>
</html>

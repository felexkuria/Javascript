<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Dashboard - Video Course Platform</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f5f5f7;
            color: #1d1d1f;
            line-height: 1.47059;
            font-weight: 400;
            letter-spacing: -0.022em;
        }
        
        .dark body {
            background: #000;
            color: #f5f5f7;
        }
        
        .navbar {
            background: white;
            border-bottom: 1px solid #d2d2d7;
            padding: 0 20px;
        }
        
        .dark .navbar {
            background: #1d1d1f;
            border-bottom-color: #424245;
        }
        
        .nav-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 64px;
        }
        
        .nav-title {
            font-size: 20px;
            font-weight: 600;
            color: #1d1d1f;
        }
        
        .dark .nav-title {
            color: #f5f5f7;
        }
        
        .nav-actions {
            display: flex;
            align-items: center;
            gap: 16px;
        }
        
        .nav-user {
            font-size: 14px;
            color: #86868b;
        }
        
        .nav-links {
            display: flex;
            gap: 8px;
        }
        
        .nav-link {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.2s ease;
        }
        
        .nav-link.active {
            background: #007aff;
            color: white;
        }
        
        .nav-link:not(.active) {
            background: #f2f2f7;
            color: #1d1d1f;
        }
        
        .dark .nav-link:not(.active) {
            background: #2c2c2e;
            color: #f5f5f7;
        }
        
        .nav-logout {
            color: #ff3b30;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 24px;
            margin-bottom: 48px;
        }
        
        .stat-card {
            background: white;
            border-radius: 18px;
            padding: 32px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }
        
        .dark .stat-card {
            background: #1d1d1f;
            box-shadow: 0 4px 20px rgba(255,255,255,0.05);
        }
        
        .stat-header {
            display: flex;
            align-items: center;
            gap: 16px;
            margin-bottom: 16px;
        }
        
        .stat-icon {
            width: 48px;
            height: 48px;
            border-radius: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: white;
        }
        
        .stat-icon.courses { background: #007aff; }
        .stat-icon.videos { background: #30d158; }
        .stat-icon.students { background: #af52de; }
        .stat-icon.completion { background: #ff9500; }
        
        .stat-content {
            flex: 1;
        }
        
        .stat-label {
            font-size: 14px;
            color: #86868b;
            margin-bottom: 4px;
        }
        
        .stat-value {
            font-size: 28px;
            font-weight: 600;
            color: #1d1d1f;
        }
        
        .dark .stat-value {
            color: #f5f5f7;
        }
        
        .content-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 32px;
            margin-bottom: 48px;
        }
        
        .content-card {
            background: white;
            border-radius: 18px;
            padding: 32px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }
        
        .dark .content-card {
            background: #1d1d1f;
            box-shadow: 0 4px 20px rgba(255,255,255,0.05);
        }
        
        .card-title {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 24px;
            color: #1d1d1f;
        }
        
        .dark .card-title {
            color: #f5f5f7;
        }
        
        .course-item {
            padding: 20px 0;
            border-bottom: 1px solid #f2f2f7;
        }
        
        .dark .course-item {
            border-bottom-color: #2c2c2e;
        }
        
        .course-item:last-child {
            border-bottom: none;
        }
        
        .course-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 12px;
        }
        
        .course-name {
            font-size: 16px;
            font-weight: 600;
            color: #1d1d1f;
            max-width: 60%;
        }
        
        .dark .course-name {
            color: #f5f5f7;
        }
        
        .course-completion {
            font-size: 20px;
            font-weight: 600;
            color: #1d1d1f;
        }
        
        .dark .course-completion {
            color: #f5f5f7;
        }
        
        .course-stats {
            font-size: 14px;
            color: #86868b;
            margin-bottom: 12px;
        }
        
        .progress-bar {
            width: 100%;
            height: 6px;
            background: #f2f2f7;
            border-radius: 3px;
            overflow: hidden;
        }
        
        .dark .progress-bar {
            background: #2c2c2e;
        }
        
        .progress-fill {
            height: 100%;
            background: #007aff;
            border-radius: 3px;
            transition: width 0.3s ease;
        }
        
        .activity-item {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 16px 0;
            border-bottom: 1px solid #f2f2f7;
        }
        
        .dark .activity-item {
            border-bottom-color: #2c2c2e;
        }
        
        .activity-item:last-child {
            border-bottom: none;
        }
        
        .activity-icon {
            width: 36px;
            height: 36px;
            border-radius: 18px;
            background: #30d158;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 16px;
        }
        
        .activity-content {
            flex: 1;
        }
        
        .activity-title {
            font-size: 14px;
            font-weight: 500;
            color: #1d1d1f;
            margin-bottom: 4px;
        }
        
        .dark .activity-title {
            color: #f5f5f7;
        }
        
        .activity-subtitle {
            font-size: 12px;
            color: #86868b;
        }
        
        .activity-date {
            font-size: 12px;
            color: #86868b;
        }
        
        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #86868b;
        }
        
        .actions-card {
            background: white;
            border-radius: 18px;
            padding: 32px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }
        
        .dark .actions-card {
            background: #1d1d1f;
            box-shadow: 0 4px 20px rgba(255,255,255,0.05);
        }
        
        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
        }
        
        .action-btn {
            padding: 16px 24px;
            border: none;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        
        .action-btn.primary {
            background: #007aff;
            color: white;
        }
        
        .action-btn.primary:hover {
            background: #0056b3;
            transform: translateY(-2px);
        }
        
        .action-btn.secondary {
            background: #f2f2f7;
            color: #1d1d1f;
            border: 1px solid #d2d2d7;
        }
        
        .dark .action-btn.secondary {
            background: #2c2c2e;
            color: #f5f5f7;
            border-color: #424245;
        }
        
        .action-btn.secondary:hover {
            background: #e5e5ea;
            transform: translateY(-2px);
        }
        
        .dark .action-btn.secondary:hover {
            background: #3a3a3c;
        }
        
        .theme-toggle {
            position: fixed;
            top: 20px;
            right: 20px;
            width: 44px;
            height: 44px;
            border: none;
            border-radius: 22px;
            background: white;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            transition: all 0.2s ease;
            z-index: 1000;
        }
        
        .dark .theme-toggle {
            background: #1d1d1f;
            box-shadow: 0 4px 20px rgba(255,255,255,0.1);
        }
        
        .theme-toggle:hover {
            transform: scale(1.1);
        }
        
        @media (max-width: 768px) {
            .nav-content {
                flex-direction: column;
                height: auto;
                padding: 16px 0;
                gap: 16px;
            }
            
            .content-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .actions-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <button class="theme-toggle" id="themeToggle">🌙</button>
    
    <nav class="navbar">
        <div class="nav-content">
            <h1 class="nav-title">Teacher Dashboard</h1>
            <div class="nav-actions">
                <span class="nav-user">Welcome, <%= user?.name || user?.email || 'Teacher' %></span>
                <div class="nav-links">
                    <a href="/student/dashboard" class="nav-link">Student View</a>
                    <a href="/dashboard" class="nav-link">Auto View</a>
                    <span class="nav-link active">Teacher View</span>
                </div>
                <a href="/logout" class="nav-logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon courses">📚</div>
                    <div class="stat-content">
                        <div class="stat-label">Total Courses</div>
                        <div class="stat-value"><%= totalCourses %></div>
                    </div>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon videos">▶️</div>
                    <div class="stat-content">
                        <div class="stat-label">Total Videos</div>
                        <div class="stat-value"><%= totalVideos %></div>
                    </div>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon students">👥</div>
                    <div class="stat-content">
                        <div class="stat-label">Active Students</div>
                        <div class="stat-value"><%= totalStudents %></div>
                    </div>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon completion">📈</div>
                    <div class="stat-content">
                        <div class="stat-label">Avg Completion</div>
                        <div class="stat-value">
                            <% 
                            const avgCompletion = courseStats.length > 0 
                                ? Math.round(courseStats.reduce((sum, course) => sum + course.completionRate, 0) / courseStats.length)
                                : 0;
                            %>
                            <%= avgCompletion %>%
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="content-grid">
            <div class="content-card">
                <h3 class="card-title">Course Performance</h3>
                <div class="course-list">
                    <% if (courseStats.length > 0) { %>
                        <% courseStats.forEach(course => { %>
                        <div class="course-item">
                            <div class="course-header">
                                <div class="course-name" title="<%= course.name %>">
                                    <%= course.name.length > 50 ? course.name.substring(0, 50) + '...' : course.name %>
                                </div>
                                <div class="course-completion"><%= course.completionRate %>%</div>
                            </div>
                            <div class="course-stats">
                                <%= course.watchedVideos %>/<%= course.totalVideos %> videos watched
                            </div>
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: <%= course.completionRate %>%"></div>
                            </div>
                        </div>
                        <% }); %>
                    <% } else { %>
                        <div class="empty-state">No course data available</div>
                    <% } %>
                </div>
            </div>

            <div class="content-card">
                <h3 class="card-title">Recent Activity</h3>
                <div class="activity-list">
                    <% if (recentActivity.length > 0) { %>
                        <% recentActivity.forEach(activity => { %>
                        <div class="activity-item">
                            <div class="activity-icon">▶️</div>
                            <div class="activity-content">
                                <div class="activity-title"><%= activity.videoTitle %></div>
                                <div class="activity-subtitle"><%= activity.courseName %></div>
                            </div>
                            <div class="activity-date">
                                <%= new Date(activity.watchedAt).toLocaleDateString() %>
                            </div>
                        </div>
                        <% }); %>
                    <% } else { %>
                        <div class="empty-state">No recent activity</div>
                    <% } %>
                </div>
            </div>
        </div>

        <div class="actions-card">
            <h3 class="card-title">Quick Actions</h3>
            <div class="actions-grid">
                <a href="/teacher/upload-center" class="action-btn primary">
                    📹 Video Upload Center
                </a>
                <a href="/admin/course-manager" class="action-btn secondary">
                    ➕ Manage Courses
                </a>
                <a href="/admin/dashboard" class="action-btn secondary">
                    📊 Analytics
                </a>
                <a href="/admin/teacher-requests" class="action-btn secondary">
                    👥 Student Management
                </a>
            </div>
        </div>
    </div>

    <script>
        // Theme management
        function initTheme() {
            const theme = localStorage.getItem('theme') || 'light';
            document.documentElement.classList.toggle('dark', theme === 'dark');
            document.getElementById('themeToggle').textContent = theme === 'dark' ? '☀️' : '🌙';
        }
        
        function toggleTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            const newTheme = isDark ? 'light' : 'dark';
            document.documentElement.classList.toggle('dark', newTheme === 'dark');
            localStorage.setItem('theme', newTheme);
            document.getElementById('themeToggle').textContent = newTheme === 'dark' ? '☀️' : '🌙';
        }
        
        initTheme();
        document.getElementById('themeToggle').addEventListener('click', toggleTheme);
    </script>
</body>
</html>
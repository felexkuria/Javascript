<style>
/* MongoDB Atlas Core Design System */
:root {
    --atlas-green: #00ED64;
    --atlas-midnight: #001E2B;
    --atlas-forest: #00684A;
    --atlas-slate: #023430;
    --atlas-white: #E7EEEB;
    --atlas-blur: blur(40px);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    background-color: var(--atlas-midnight);
    color: var(--atlas-white);
    font-family: 'Euclid Circular A', 'Inter', system-ui, sans-serif;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
}

/* Premium Technical Grid Background */
.learning-canvas {
    background-image: radial-gradient(circle at 80% 20%, rgba(0, 237, 100, 0.05), transparent 40%),
                      linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
    background-size: 100% 100%, 40px 40px, 40px 40px;
}

/* MongoDB Atlas Card Architecture */
.atlas-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
}

.atlas-card.glass {
    backdrop-filter: var(--atlas-blur);
    background: rgba(2, 52, 48, 0.3);
}

.atlas-card:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(0, 237, 100, 0.3);
    transform: translateY(-4px);
    box-shadow: 0 40px 80px rgba(0,0,0,0.5);
}

/* Command Center Components */
.technical-label {
    font-family: 'SF Mono', 'Fira Code', monospace;
    font-size: 10px;
    font-weight: 700;
    color: #9FB1AD;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    opacity: 0.8;
}

.atlas-badge {
    background: rgba(0, 237, 100, 0.1);
    color: var(--atlas-green);
    padding: 6px 14px;
    border-radius: 8px;
    font-size: 10px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    border: 1px solid rgba(0, 237, 100, 0.2);
    display: inline-flex;
    align-items: center;
    gap: 8px;
}

/* Scroll Customization */
.scroll-premium::-webkit-scrollbar {
    width: 6px;
}

.scroll-premium::-webkit-scrollbar-track {
    background: transparent;
}

.scroll-premium::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    border: 1px solid transparent;
    background-clip: content-box;
}

.scroll-premium::-webkit-scrollbar-thumb:hover {
    background: var(--atlas-green);
}

/* Navigation & Sidebar Consistency */
.atlas-nav {
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    background: rgba(0, 30, 43, 0.95);
    backdrop-filter: var(--atlas-blur);
}

.lecture-item {
    padding: 18px 24px;
    border-radius: 18px;
    margin-bottom: 12px;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.03);
    display: flex;
    align-items: center;
    gap: 18px;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.lecture-item:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.1);
    transform: translateX(4px);
}

.lecture-item.active {
    background: rgba(0, 237, 100, 0.08);
    border-color: rgba(0, 237, 100, 0.3);
    box-shadow: inset 0 0 20px rgba(0, 237, 100, 0.05);
}

.lecture-icon {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.05);
    color: #9FB1AD;
}

.lecture-item.active .lecture-icon {
    background: var(--atlas-green);
    color: var(--atlas-midnight);
    border-color: var(--atlas-green);
}

/* Pulse Animations for AI */
.velocity-pulse {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--atlas-green);
    box-shadow: 0 0 10px var(--atlas-green);
    animation: atlas-heartbeat 2s infinite;
}

@keyframes atlas-heartbeat {
    0% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.5); opacity: 0.4; }
    100% { transform: scale(1); opacity: 1; }
}
</style>
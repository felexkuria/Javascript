<div class="video-section animate-slide-up">
    <div class="video-container" id="videoContainer" style="position: relative; width: 100%; aspect-ratio: 16/9; background: #000; border-radius: 12px; overflow: hidden; box-shadow: 0 10px 30px rgba(0,0,0,0.5);">
        <% if (isYouTube) { %>
        <iframe id="youtubePlayer" 
                src="<%= video.embedUrl %>" 
                style="width: 100%; height: 100%; position: absolute; top: 0; left: 0; border: none; object-fit: cover;"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                allowfullscreen>
        </iframe>
        <% } else if (video && (video.isPdf || video.type === 'pdf' || (video.videoUrl && video.videoUrl.toLowerCase().includes('.pdf')) || (video.title && video.title.toLowerCase().includes('pdf')))) { %>
        <div class="pdf-viewer-container" style="width: 100%; height: 100%; position: absolute; top: 0; left: 0; background: #1e1e24; display: flex; flex-direction: column;">
            <iframe src="<%= video.fullVideoUrl || video.videoUrl || video.url %>" 
                    style="flex: 1; border: none; width: 100%; height: 100%;"
                    id="pdfViewer">
            </iframe>
            <div class="pdf-fallback" style="padding: 12px 20px; background: rgba(0,0,0,0.9); color: white; display: flex; justify-content: space-between; align-items: center; font-size: 14px; border-top: 1px solid #333;">
                <span>View document offline or bypass browser PDF viewer:</span>
                <a href="<%= video.fullVideoUrl || video.videoUrl || video.url %>" download="<%= video.title %>.pdf" target="_blank" style="color: #00ED64; text-decoration: none; font-weight: 600; padding: 4px 12px; background: rgba(0,237,100,0.1); border-radius: 6px;">Download PDF ↓</a>
            </div>
        </div>
        <% } else { %>
        <video id="videoPlayer" controls crossorigin="anonymous" class="interactive-video" preload="metadata" 
               poster="<%= video.thumbnailUrl || '/thumbnails/' + (video._id || video.videoId) + '.jpg' %>"
               onerror="window.handleVideoError(this)"
               style="width: 100%; height: 100%; position: absolute; top: 0; left: 0; object-fit: contain; background: #000; border: none; outline: none;">
            <source src="<%= video.fullVideoUrl || video.videoUrl || video.url %>" type="video/mp4" id="videoSource" onerror="window.handleSourceError(this)">

            <% if (video.captionsUrl) { %>
            <track label="English" kind="subtitles" srclang="en" src="/captions/<%= courseName %>/<%= video._id %>" default />
            <% } %>
            <track id="autoCaption" label="Auto-Generated" kind="subtitles" srclang="en" 
                   src="/api/captions/vtt/<%= encodeURIComponent(courseName) %>/<%= video._id %>" />
        </video>
        <% } %>
        
        <!-- Cinematic Neural Thumbnail Overlay (Replacing basic poster) -->
        <% if (!isYouTube && !video.isPdf && video.type !== 'pdf' && !(video.title && video.title.toLowerCase().includes('pdf'))) { %>
        <div class="video-poster-overlay" id="posterOverlay" 
             style="background-image: url('<%= video.thumbnailUrl || '/thumbnails/' + (video._id || video.videoId) + '.jpg' %>');">
            <div class="poster-vignette"></div>
            <!-- The Play Button lives in the overlay now for maximum z-index pop -->
            <div class="video-overlay" id="videoOverlay" style="display: flex; opacity: 1; pointer-events: all; position: absolute; inset: 0; z-index: 20;">
                <div class="play-button" id="playButton" style="cursor: pointer; transform: scale(1);">
                    <span>▶</span>
                </div>
            </div>
        </div>
        <% } %>
        
        <!-- Interactive overlay (legacy fallback for pause state) -->
        <% if (!isYouTube && (video.isPdf || video.type === 'pdf')) { %>
        <div class="video-overlay" id="videoOverlay" style="display: none;"></div>
        <% } %>
        
        <% if (!video.isPdf && video.type !== 'pdf' && !(video.title && video.title.toLowerCase().includes('pdf'))) { %>
        <div id="mediaErrorOverlay" class="hidden hud-backdrop" 
             style="background-image: url('<%= video.thumbnailUrl || '/thumbnails/' + (video._id || video.videoId) + '.jpg' %>');">
            <div class="hud-vignette"></div>
            <div class="hud-grid"></div>
            <div class="hud-scanner"></div>
            
            <div class="neural-core-container">
                <!-- Center the play button on top of the neural core -->
                <div class="play-button hud-integrated-play" onclick="window.startProtocol()" style="z-index: 200; cursor: pointer;">
                    <span>▶</span>
                </div>
                <div class="neural-ring ring-1"></div>
                <div class="neural-ring ring-2"></div>
                <div class="neural-ring ring-3"></div>
                <div class="neural-nucleus"></div>
            </div>

            <h3 class="premium-title text-2xl tracking-[0.3em] uppercase mb-2">SYNC ENGINE</h3>
            <p class="sync-status-msg">Calibrating AI Learning Nodes...</p>

            <div class="hud-telemetry">
                <div class="telemetry-node">
                    <div class="node-label">Curriculum</div>
                    <div class="node-value"><%= courseName.replace(/[-_]/g, ' ') %></div>
                </div>
                <div class="telemetry-node">
                    <div class="node-label">Deployment Node</div>
                    <div class="node-value"><%= (video.title || 'ARCHITECT SECTOR').substring(0, 24) %></div>
                </div>
                <div class="telemetry-node">
                    <div class="node-label">Instructor</div>
                    <div class="node-value">Engineer Felex</div>
                </div>
                <div class="telemetry-node">
                    <div class="node-label">Asset Duration</div>
                    <div class="node-value"><%= (video.isPdf || video.type === 'pdf' || (video.title && video.title.toLowerCase().includes('pdf'))) ? 'STATIC RESOURCE' : (video.duration || '24:00') %></div>
                </div>
            </div>

            <div id="hudLogs" class="hud-logs hidden">
                <!-- Appended via JS -->
            </div>

            <div class="mt-12 flex gap-6" id="hudControls">
                <button onclick="window.repairAndReload('<%= courseName %>')" class="btn-hud-retry px-10 py-4 rounded-xl text-[10px] font-black tracking-[0.2em] uppercase">
                    REINITIATE HANDSHAKE
                </button>
            </div>
        </div>
        <% } %>

        <script>
        (function() {
            window.handleVideoError = function(video) {
                console.error("Video element reported error:", video.error);
                showErrorOverlay();
            };

            window.handleSourceError = function(source) {
                console.error("Video source failed to load:", source.src);
                showErrorOverlay();
            };

            window.repairAndReload = async function(courseName) {
                const btn = event.currentTarget;
                const controls = document.getElementById('hudControls');
                const logContainer = document.getElementById('hudLogs');
                
                btn.innerText = 'DISCOVERING...';
                btn.disabled = true;
                logContainer.classList.remove('hidden');
                logContainer.innerHTML = '<div class="hud-log-entry text-[#00ED64]">📡 ACCESSING DEEP ARCHIVE LAYER...</div>';
                
                try {
                    const response = await fetch('/api/sync/repair-manifest', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ courseName })
                    });
                    const result = await response.json();
                    
                    // Progressive Log Output for High-Fidelity UX
                    if (result.logs) {
                        for (const log of result.logs) {
                            const entry = document.createElement('div');
                            entry.className = 'hud-log-entry';
                            entry.innerText = log;
                            logContainer.appendChild(entry);
                            logContainer.scrollTop = logContainer.scrollHeight;
                            await new Promise(r => setTimeout(r, 200)); // Cinematic delay
                        }
                    }

                    if (result.success) {
                        const overlay = document.getElementById('mediaErrorOverlay');
                        overlay.classList.add('calibration-complete');
                        
                        // Update Video Source immediately if URL was repaired
                        if (result.newUrl) {
                            const video = document.getElementById('videoPlayer');
                            const source = document.getElementById('videoSource');
                            if (source) source.src = result.newUrl;
                            if (video) video.load();
                        }

                        // Replace retry with START PROTOCOL
                        controls.innerHTML = `
                            <button onclick="window.startProtocol()" class="btn-hud-start px-12 py-5 rounded-xl text-[12px] font-black tracking-[0.3em] uppercase">
                                START PROTOCOL
                            </button>
                        `;
                    } else {
                        throw new Error(result.error || 'Sync failed');
                    }
                } catch (error) {
                    const entry = document.createElement('div');
                    entry.className = 'hud-log-entry text-red-500';
                    entry.innerText = 'CRITICAL: ' + error.message;
                    logContainer.appendChild(entry);
                    btn.innerText = 'RETRY HANDSHAKE';
                    btn.disabled = false;
                }
            };

            window.startProtocol = function() {
                const overlay = document.getElementById('mediaErrorOverlay');
                const video = document.getElementById('videoPlayer');
                
                overlay.style.transition = 'opacity 0.8s ease-out, transform 0.8s ease-out';
                overlay.style.opacity = '0';
                overlay.style.transform = 'scale(1.1)';
                
                setTimeout(() => {
                    overlay.classList.add('hidden');
                    document.body.classList.remove('hud-active');
                    if (video) {
                        video.style.display = 'block';
                        video.play().catch(e => console.log("Autoplay blocked, user interaction required"));
                    }
                }, 800);
            };

            function showErrorOverlay() {
                // Universal PDF Suppression Guard (Backend Type + Title + URL Verification)
                <% if (video && (video.isPdf || video.type === 'pdf' || (video.title && video.title.toLowerCase().includes('pdf')) || (video.videoUrl && video.videoUrl.toLowerCase().split('?')[0].endsWith('.pdf')))) { %>
                return;
                <% } %>

                const overlay = document.getElementById('mediaErrorOverlay');
                if (overlay) overlay.classList.remove('hidden');
                
                // Trigger global HUD orchestration mode
                document.body.classList.add('hud-active');

                // Also hide the poster and default controls
                const vp = document.getElementById('videoPlayer');
                if (vp) {
                    vp.style.display = 'none';
                }
            }

            document.addEventListener('DOMContentLoaded', function() {
                const videoPlayer = document.getElementById('videoPlayer');
                const overlay = document.getElementById('videoOverlay');
                const posterOverlay = document.getElementById('posterOverlay');
                const playButton = document.getElementById('playButton');
                
                // Auto-trigger error check for stalled streams
                if (videoPlayer) {
                    const timeout = setTimeout(() => {
                        if (videoPlayer.networkState === HTMLMediaElement.NETWORK_NO_SOURCE) {
                            showErrorOverlay();
                        }
                    }, 5000);
                    
                    videoPlayer.addEventListener('loadeddata', () => clearTimeout(timeout));
                }

                if (videoPlayer && playButton) {
                    playButton.addEventListener('click', function(e) {
                        e.stopPropagation();
                        videoPlayer.play().catch(err => {
                            console.error("Play blocked:", err);
                            showErrorOverlay();
                        });
                    });
                    
                    videoPlayer.addEventListener('play', function() {
                        if (overlay) {
                            overlay.style.opacity = '0';
                            overlay.style.pointerEvents = 'none';
                        }
                        if (posterOverlay) {
                            posterOverlay.classList.add('fade-out');
                        }
                    });
                    
                    videoPlayer.addEventListener('pause', function() {
                        if (overlay) {
                            overlay.style.opacity = '1';
                            overlay.style.pointerEvents = 'all';
                        }
                        // We keep posterOverlay hidden on pause to show the actual frame
                    });

                    videoPlayer.addEventListener('ended', function() {
                        if (posterOverlay) {
                            posterOverlay.classList.remove('fade-out');
                        }
                        if (overlay) {
                            overlay.style.opacity = '1';
                            overlay.style.pointerEvents = 'all';
                        }
                    });

                    videoPlayer.addEventListener('click', function() {
                        if (videoPlayer.paused) videoPlayer.play();
                        else videoPlayer.pause();
                    });
                }
            });
        })();
        </script>
    </div>
</div>